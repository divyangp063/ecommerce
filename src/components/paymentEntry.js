import React from "react";

const purchaseEntry = () => {
  let title = "payment page";

  return (
    <div>
      <h1>
        {title}
        </h1>
    </div>
  );
};

export default purchaseEntry;