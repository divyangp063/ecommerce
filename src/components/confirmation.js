import React from "react";

const confirmation = () => {
  let title = "order confirmation page";

  return (
    <div>
      <h1>
        {title}
        </h1>
    </div>
  );
};

export default confirmation;